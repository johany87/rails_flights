<div class="container">
    <h1 class="text-center">Edit City</h1>
</div>

<div class="container">

  <%= form_for @city do |c| %>

    <div class="form-group">
      <%= c.label :name %>
      <%= c.text_field :name, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= c.label :country_name %>
      <%= c.text_field :country_name, class: "form-control" %>
    </div>

    <div ccass="form-group">
      <%= c.label :time_zone %>
      <%= c.text_field :time_zone, class: "form-control" %>
    </div>

    <%= c.submit "Update City", class:"btn btn-primary" %>

  <% end %>
</div>

<div class="container">
  <% if @city.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@city.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @city.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
</div>
